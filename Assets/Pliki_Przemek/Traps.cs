using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Traps : MonoBehaviour
{

    static public int trapId = 0; //id pulapki
    static public int fireTrapActive = 1; //czy pulapka fire jest aktywna
    static public int needleTrapActive = 1; //czy pulapka needle jest aktywna
    static public int cutterTrapActive = 1; //czy pulapka cutter jest aktywna

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
