using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Traps : MonoBehaviour
{

    static public int trapId = 0;
    static public int fireTrapActive = 1;
    static public int needleTrapActive = 1;
    static public int cutterTrapActive = 1;






    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
